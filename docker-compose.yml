version: "3.9"
   
services:
  db:
    image: postgres:13-alpine
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./medusa_light:/medusa_light_project
      - ./data_static/web:/vol/web
    environment:
      - SECRET_KEY=django-insecure-q&j246(1x2-zymmkhv+j*zdmw22_q2+l-(zhc$cvu2-=sp0(@8
      - DEBUG=1
      - DB_HOST=db
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASS=postgres
    ports:
      - "8000:8000"
    depends_on:
      - db
